# Contributing

1. Fork the repo
2. `cp config.example.yml config.yml`
3. `make check` before opening a PR

Keep patches small. Match the style already in the tree.

Tags for releases look like `release-0.1.0`.
